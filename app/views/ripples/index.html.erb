<p id="notice"><%= notice %></p>

<h1>Ripples</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Url</th>
      <th>Message</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @ripples.each do |ripple| %>
      <tr>
        <td><%= ripple.name %></td>
        <td><%= ripple.message.truncate(50, omission: '...') %></td>
        <td><%= ripple.created_at %></td>
        <td><%= link_to 'Show', ripple %></td>        
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Ripple', new_ripple_path %>
